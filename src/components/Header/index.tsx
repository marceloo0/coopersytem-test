import React from 'react';

import {Container, Title} from './styles';

const Header: React.FC = () => {
  return (
    <Container>
      <Title>Resgate</Title>
    </Container>
  );
};

export default Header;
